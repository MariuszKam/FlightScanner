<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.persistence.interfaces.FlightRepository">

    <insert id="create">
        INSERT INTO flight (id, name, start_id, destination_id, airline_id, price)
        VALUES (#{id}, #{name}, #{start}, #{destination}, #{airline}, #{price})
    </insert>

    <select id="loadById" resultMap="FlightResultMap">
        SELECT * FROM flight WHERE id = #{id}
    </select>

    <select id="loadAll" resultMap="FlightResultMap">
        SELECT * FROM flight
    </select>

    <resultMap id="FlightResultMap" type="com.solvd.model.Flight" autoMapping="false">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="price" column="price"/>
        <association property="start" resultMap="com.solvd.persistence.interfaces.AirportRepository.AirportResultMap"/>
        <association property="destination" resultMap="com.solvd.persistence.interfaces.AirportRepository.AirportResultMap"/>
        <association property="airline" resultMap="com.solvd.persistence.interfaces.AirlineRepository.AirlineResultMap"/>
    </resultMap>
</mapper>